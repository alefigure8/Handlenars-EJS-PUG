<div class="container">
  <div class="row">
    <div class="col-md-8 mr-auto ml-auto mt-5">
        <h2 class="mb-5 text-uppercase text-center">Vista de Productos</h2>
      {{#if productosLista}}
        <table class="table">
          <thead>
             <tr>
              <th scope="col">Título</th>
              <th scope="col">Precio</th>
              <th scope="col">Foto</th>
            </tr>
          </thead>
          <tbody>
           {{#each productosLista}}
            <tr>
              <td>{{title}}</td>
              <td>${{price}}</td>
              <td><img src="{{thumbnail}}" class="img-thumbnail img" alt="{{title}}"></td>
            </tr>
          {{/each}}
          </tbody>
        </table>
        {{else}}
          <div class="alert alert-warning">
            No hay productos. Comience creando alguno <a href="/">Click!</a>
          </div>
        {{/if}}
      <a href="/" class="btn btn-dark mt-3">Formulario</a>
    </div>
  </div>
</div>
